// Re-usable modal for 3rd party auth logins here
// Initial implementation will be for the Facebook Login
import React from "react";

// Will need to accept multiple props coming from login page (where the 3rd party logins will take place)
const socialAuthModal = () => {
  return;
};

export default socialAuthModal;
